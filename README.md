# RustCV

> Rust Computer Vision Library

### **Ubuntu**

```bash
# 1. 更新 apt 缓存
sudo apt update

# 2. 安装 libclang 和 llvm 开发包 必要的系统库
sudo apt install llvm-dev libclang-dev clang libv4l-dev libxkbcommon-dev libwayland-dev libx11-dev
```
